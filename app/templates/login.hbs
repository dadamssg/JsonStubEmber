<div class="row">
    <div class="col-md-4"></div>
    <div class="col-md-4">
        <div class="login-container">
            
            <div class="alert alert-warning alert-dismissible" role="alert">
              <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
              <strong>Announcements!</strong>
              <br/><br/>
               JsonStub has just been updated. In doing that, the authentication system has been updated as well. If you signed up before 9/14/2014, you will need to {{#link-to 'reset'}}reset your password.{{/link-to}} Sorry for the inconvience! 

               <br/><br/>
               Also, the <code>Content-Type: application/json</code> header must always be present now for any stubbed requests.

              <br/><br/><small>If you used mailinator, shoot <a href="mailto:support@jsonstub.com" target="_blank">support</a> an email with the email you'd like to update with!</small>
            </div>
            
            {{messages-widget for=this}}

            <form {{action 'authenticate' on='submit'}} role="form">
                <div class="form-group">
                    <label for="identification">Email</label>
                    {{input type="email" required="true" value=identification class="form-control" id="identification" placeholder="Email"}}
                </div>
                <div class="form-group">
                    <label for="password">Password</label>        
                    {{input type="password" required="true" value=password class="form-control" id="password" placeholder="Password"}}
                </div>
                <div class="row">
                    <div class="col-md-6">
                        <button type="submit" class="btn btn-primary ladda-button login-button" {{bind-attr disabled=submitting}} data-style="expand-left" data-spinner-size="25">
                            <span class="ladda-label">
                                {{#if submitting}}
                                    Logging in
                                {{else}}
                                    Login
                                {{/if}}
                            </span>
                        </button>
                    </div>
                    <div class="col-md-6 text-right">{{#link-to 'reset'}}Forget your password?{{/link-to}}</div>
                </div>
            </form>
        </div>
    </div>
    <div class="col-md-4"></div>
</div>

{{partial "partials/footer"}}